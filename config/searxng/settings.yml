use_default_settings: true

general:
  debug: false
  instance_name: "SierraNueva private search radar"
  enable_metrics: false

search:
  safe_search: 1
  autocomplete: ""
  default_lang: "es-ES"
  formats:
    - json

server:
  limiter: false
  public_instance: false
  image_proxy: false

outgoing:
  request_timeout: 6.0
  max_request_timeout: 10.0
